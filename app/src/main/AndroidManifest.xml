<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="oddtimeworks.com.franken">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activity02"></activity>
        <activity android:name=".Activity03"></activity>
        <activity android:name=".Activity04"></activity>
        <activity android:name=".Activity05"></activity>
        <activity android:name=".Activity06"></activity>
        <activity android:name=".Activity07"></activity>
        <activity android:name=".Activity08"></activity>
        <activity android:name=".Activity09"></activity>
        <activity android:name=".Activity10"></activity>
    </application>

</manifest>
